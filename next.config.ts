import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: {
  //   appDir: true, // Other experimental features you want to enable
  // },
};

export default nextConfig;
